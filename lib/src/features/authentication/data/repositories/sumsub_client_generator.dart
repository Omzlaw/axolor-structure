import 'dart:convert';
import 'package:axolor_mobile/src/core/utils/services/network_service.dart';
import 'package:dio/dio.dart';
import 'package:flutter/services.dart';

class SumsubClientGenerator {
  final NetworkService _networkService = NetworkService();

  Future generateToken() async {
    try {
      // Call API
    } catch (e) {
      // Handle errors
    }
  }
}
